# 端口

一台计算机会开放若干端口，编号为 `0-65535`。

向一台计算机发送数据时要指明送至的端口。

一个端口允许至多一个应用监听，发送至某个端口数据会分配给监听此端口的应用。

- 端口 `0-1023` 一般被分配给特定的服务，例如 HTTP 服务等，称为知名端口。
- 端口 `1024-49151` 为注册端口，一般被分配给用于用户和公司的应用程序，可以自由使用。
- 端口 `49152-65535` 为动态端口，通常被不需要固定端口号的应用程序占用，例如 TCP 连接的发起方。
